#include "main.h"
/**
 * print_most_numbers - entry point
 *
 * Return: 0
 */
void print_most_numbers(void)
{
	int x;

	{
		for (x >= 0; x <= 9; x != 2; x != 4; x++)
			_putchar ('x');
	}
	_putchar ('x');

	return (0);
}
